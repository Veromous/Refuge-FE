# Google OAuth client ID (public value, safe to expose in the browser).
# Must match GOOGLE_CLIENT_ID on the backend and have your frontend origin
# (e.g. http://localhost:5173) listed as an authorized JavaScript origin.
VITE_GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com
